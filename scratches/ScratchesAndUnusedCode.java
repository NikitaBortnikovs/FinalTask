package finaltask.scratches;

public class ScratchesAndUnusedCode {
    //        softAssert.assertEquals(productModel.productPrice, actualPriceEnd);
    //$$(By.xpath("//li[@class='submenu-lvl1__list-item color-theme-22 submenu-lvl1__list-item--has-child']")).last().click();
//a class main-menu__handle

//
//        softAssert.assertAll();
//cartPage1a.clickOnDeliveryOption();
//public void  clickOnCookieButAllow () {driver.findElement(cookieButt).click();}
//        String actualPriceStart = priceStart.getText();
//        String actualPriceEnd = priceEnd.getText();
//
//        softAssert.assertEquals(actualPriceStart, actualPriceEnd);
//
//        softAssert.assertAll();
    //homePage1a.clickAcceptCookies();
    //js.executeScript("window.scrollBy(0,500)", "");
    //Thread.sleep(6000);
    //driver.switchTo().alert().dismiss();

//        if (priceStart.equals(priceEnd)) {
//            System.out.println("Price at the end and at the beginning is the same");
//        } else {
//            System.out.println("Price at the end and at the beginning is not the same - SOMETHING WENT WRONG");
//        }
//homePage1a.clickAcceptCookies();

//        WebElement nameAtTheEnd = driver.findElement(By.xpath("//div[@class='name']")).getText();
//        System.out.println("Customer name and surname and the end -  " + nameAtTheEnd);


//        if (softAssert.assertEquals(actualPriceStart, actualPriceEnd)) {
//            System.out.println("Price at the end and at the beginning is the same");
//        } else {
//            System.out.println("Price at the end and at the beginning is not the same - SOMETHING WENT WRONG");
//        }
//        softAssert.assertAll();
    //js.executeScript("window.scrollBy(0,500)", "");
    //Thread.sleep(6000);
    //driver.switchTo().alert().dismiss();

//        WebElement priceStart = driver.findElement(By.xpath("//p[@class='detailed-cart-item__total']"));
////        System.out.println("Product price and the begining -  "+ priceStart.getText());
    //js.executeScript("window.scrollBy(0,500)", "");
    //homePage1a.clickAcceptCookies();
    //Thread.sleep(2000);

    //js.executeScript("window.scrollBy(0,400)", "");


}
